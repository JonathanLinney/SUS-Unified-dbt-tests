version: 2

models:
  - name: provider_daily_apc_activity_DBT
    description: "Monthly activity by provider"
    columns:

      - name: provider
        description: "Provider identifier"
        tests:
          - not_null
          - unique

      - name: activity_date
        description: "Date of activity"
        tests:
          - not_null
          
      - name: records
        description: "Count of records"
        tests:
          - not_null
